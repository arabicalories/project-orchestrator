{
  "intake": {
    "kind": "transition",
    "targetStage": "plan",
    "reason": "shared skill policy: intake complete, move to planning"
  },
  "plan": {
    "artifactPrereq": "plan",
    "onMissingArtifact": {
      "kind": "write_artifact",
      "artifactName": "plan",
      "summary": "plan generated by shared skill policy"
    },
    "decisionAware": true,
    "whenUserDecisionPresent": {
      "kind": "transition",
      "targetStage": "await_user_approval",
      "reason": "shared skill policy: plan ready and approval is required"
    },
    "default": {
      "kind": "transition",
      "targetStage": "codex_run",
      "reason": "shared skill policy: plan ready and no approval required"
    }
  },
  "await_user_approval": {
    "kind": "wait",
    "reason": "shared skill policy: waiting for user approval"
  },
  "codex_run": {
    "executorAware": true,
    "whenNoSession": {
      "kind": "start_executor",
      "reason": "shared skill policy: start codex executor"
    },
    "artifactPrereq": "codexRunRecord",
    "onMissingArtifact": {
      "kind": "sync_executor_run_record",
      "artifactName": "codexRunRecord",
      "summary": "codex run record collected by shared skill policy"
    },
    "default": {
      "kind": "transition",
      "targetStage": "collect",
      "reason": "shared skill policy: codex run record ready"
    }
  },
  "collect": {
    "executorAware": true,
    "artifactPrereq": "codexSummary",
    "onMissingArtifact": {
      "kind": "collect_executor_outputs",
      "artifactName": "codexSummary",
      "summary": "codex summary collected by shared skill policy"
    },
    "default": {
      "kind": "transition",
      "targetStage": "review",
      "reason": "shared skill policy: collect completed with codex summary"
    }
  },
  "review": {
    "artifactPrereq": "review",
    "onMissingArtifact": {
      "kind": "run_review",
      "artifactName": "review",
      "summary": "review executor should produce artifact and next-stage decision"
    },
    "default": {
      "kind": "stop",
      "reason": "shared skill policy: review completion must be decided by review executor, not by default stage transition"
    }
  },
  "await_user_decision": {
    "kind": "wait",
    "reason": "shared skill policy: waiting for user result-side decision"
  },
  "fixback": {
    "kind": "transition",
    "targetStage": "codex_run",
    "reason": "shared skill policy: fixback returns task to codex_run"
  },
  "pr_ready": {
    "deliveryClosureAware": true,
    "onMissingDeliveryClosure": {
      "kind": "set_delivery_closure",
      "closureKind": "pr-created",
      "detail": "fake PR created by shared skill policy"
    },
    "default": {
      "kind": "transition",
      "targetStage": "done",
      "reason": "shared skill policy: delivery closure present"
    }
  },
  "done": {
    "kind": "stop",
    "reason": "shared skill policy: task is done"
  },
  "blocked": {
    "kind": "stop",
    "reason": "shared skill policy: task is blocked"
  },
  "failed": {
    "kind": "stop",
    "reason": "shared skill policy: task failed"
  }
}
